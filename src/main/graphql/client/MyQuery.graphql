query BookDetail($id:ID!) {
    bookById(id: $id) {
        id
        name
    }
}
query BookDetails {
    findAllBooks {
        id
        name
        pageCount
        author {
            firstName
            lastName
        }
    }
}

